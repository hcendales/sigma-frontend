import { MsgErrorDirective } from './msg-error.directive';

describe('MsgErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgErrorDirective();
    expect(directive).toBeTruthy();
  });
});
